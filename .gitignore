# Secrets and local env (never commit)
.env
.env.local
.env.*.local
.env.production
.env.development
web/.env
web/.env.local
web/.env*.local
**/.env
**/.env.local
**/.env.*.local
*.pem
*.key
id_rsa
id_ed25519
credentials.json
service-account*.json

# Dependencies and build
node_modules/
web/node_modules/
web/.next/
.next/
dist/
out/
*.tsbuildinfo
web/tsconfig.tsbuildinfo

# WhatsApp Web.js session (contains browser profile / auth)
.wwebjs_auth/
.wwebjs_cache/
.wwebjs_auth/**
.wwebjs_cache/**

# Supabase local temp / link state (keep migrations + config.toml)
supabase/.temp/
supabase/.branches/

# Agent session notes / local task logs (never public)
tasks/
tasks/**
**/todo.md

# Private / non-public materials (never ship to the public repo)
.video-analysis/
.video-analysis/**
docs/INVESTOR-MAP.md
docs/*investor*
docs/*INVESTOR*
docs/ENGINEER-ONBOARDING.md
docs/username-send-phase1.md
# Product OS / strategy (private). Architecture + ops stay public when useful.
docs/NORTH-STAR.md
docs/PRODUCT-STRATEGY.md
docs/ROADMAP.md
docs/DECISIONS.md
docs/FUTURE-SYSTEMS.md
docs/README.md
docs/demo-scripts/
docs/demo-scripts/**
# Local screen captures and demo edit intermediates
*.mp4
Captures/
.video-edit/
flizy-demo-edit/

# Logs / OS
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp

# Foundry: build + forge broadcast can include deploy receipts; keep addresses in deployments/
contracts/out/
contracts/cache/
contracts/broadcast/
contracts/cache/**
contracts/broadcast/**
# forge-std is vendored via submodule/path; do not require committing broadcast
# contracts/lib/ may be present locally; prefer lockfile + install
contracts/lib/
