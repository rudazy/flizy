# web/ is a separate npm project, so the root .npmrc does not reach it.
# Without this line an install here would skip the supply-chain age gate that
# the root project has, which is exactly when a freshly published malicious
# version is most dangerous.
min-release-age=7
